[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auth-log-scan"
version = "0.1.0"
description = "Scan OpenSSH auth logs for brute-force, user enumeration, and suspicious logins"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
# Runtime uses the standard library only (re, argparse, datetime, json, collections).
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<9",
]

[project.scripts]
auth-log-scan = "auth_log_scan.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

# The page generator reads its template and assets from beside itself, so they have to
# travel with the package rather than only existing in the source tree.
[tool.setuptools.package-data]
"auth_log_scan.site" = ["templates/*.html", "assets/*.css", "assets/*.js"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
